signature Functor ::= ∀f : * -> *.
  { fmap :: ∀α β. (α -> β) -> f α -> f β
  }

signature Applicative ::= ∀f : * -> *.
  { pure :: ∀α. α -> f α
  }

signature Monad ::= ∀m : * -> *.
  { bind :: ∀α β. (α -> m β) -> m α -> m β
  }
