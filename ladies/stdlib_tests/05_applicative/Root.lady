use Stdlib.
(* Applicative apply (Perhaps) -- expect 42 *)
start :: Int -> Unit := λ_.
  print_endline "##TC"
  deconstruct apply (This (λx. x + 1)) (This 41) into
    This n -> print_endline (prim_show n)
  | Nope   -> print_endline "no"
