use Stdlib.

witness Monoid Int :=
  { mempty  := 0
    mappend := λp q. p + q
  }

sum_list :: List Int -> Int :=
  λys.
    fold_map id ys

start := λ_.
  let xs = List.tabulate 44 id in
  print_endline "hi, mom: `sum_list xs`"
