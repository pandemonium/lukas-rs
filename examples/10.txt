factorial :: Int -> Int :=
  lambda n.
    deconstruct n into
      0 -> 1
    | x ->
        let arg = x - 1
        in
            x * factorial arg

start := lambda x.
  print_endline "starting"
  let ans = factorial 20
  in print_endline (show ans)
