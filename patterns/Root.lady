List ::= ∀α. Cons α (List α) | Nil

map :: ∀α β. (α -> β) -> List α -> List β := λf xs.
  deconstruct xs into
    Cons a xs -> Cons (f a) (map f xs)
  | Nil       -> Nil

start := λ_.
  map (λx. 1 + 1) (Cons 1 Nil)
